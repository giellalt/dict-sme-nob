repos:
  - repo: local
    hooks:
      - id: xmllint
        name: run xmllint --valid
        entry: .githooks/pre-commit-xmllint-validate.py
        language: python
        files: '^src/.*xml$'
        exclude: '^src/meta\.xml$'

      - id: normalize-unicode
        name: Normalize Unicode in XML files
        entry: .githooks/pre-commit-normalize-unicode.py
        language: python
        files: '^src/.*xml$'
        exclude: '^src/meta\.xml$'
